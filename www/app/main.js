define(function (require) {
    // Load any app-specific modules
    // with a relative require call,
    // like:
    // var messages = require('./messages');

    // Load library/vendor modules using
    // full IDs, like:
    // require("kotlin");
    // require("long");
    // require("protobufjs/light");
    require("pbandk-runtime-js");
    console.log("loaded?");
});
